const initialState = {};

const GlobalState = (state = initialState, action) => {
  switch (action) {
    case action:
      return state;
    default:
      return state;
  }
};

export default GlobalState;
