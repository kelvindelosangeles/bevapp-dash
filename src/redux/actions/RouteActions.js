import moment from "moment";
import uuid from "uuid";
import store from "store";

export const completeRoute = () => {
    return (dispatch, getState) => {
        console.log("Complete Route initirated");
    };
};
