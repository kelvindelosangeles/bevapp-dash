import React from "react";
import styled from "styled-components";

const CompletedOrders = () => {
  return <Heading>Completed Orders</Heading>;
};

const Heading = styled.h3`
  font-size: 24px;
  padding: 32px;
  font-family: Poppins-Medium;
  font-weight: unset;
`;

export default CompletedOrders;
